#
# doc_build.yml
#
# Copyright The OBDH 2.0 Contributors.
#
# OBDH 2.0
#
# This work is licensed under the Creative Commons Attribution-ShareAlike 4.0
# International License. To view a copy of this license,
# visit http://creativecommons.org/licenses/by-sa/4.0/.
#

name: Documentation Build

on:
  push:
    branches: [dev_doc]
  pull_request:
    branches: [master, dev, dev_doc]

jobs:
  build_user_manual_html:
    name: Build User Manual HTML
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v6

      - uses: ammaraskar/sphinx-action@master
        with:
          docs-folder: "doc/user_manual"
          pre-build-command: "pip install -r doc/user_manual/requirements.txt --upgrade pip"

      - uses: actions/upload-artifact@v7
        with:
          name: documentation_html
          path: doc/user_manual/_build/html/

  build_user_manual_pdf:
    name: Build User Manual PDF
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install LaTeX dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            latexmk \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-lang-english \
            texlive-fonts-extra

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install sphinx

          if [ -f doc/user_manual/requirements.txt ]; then
            pip install -r doc/user_manual/requirements.txt
          fi

      - name: Build PDF documentation
        run: make -C doc/user_manual latexpdf

      - name: Upload PDF artifact
        uses: actions/upload-artifact@v7
        with:
          name: documentation_pdf
          path: doc/user_manual/_build/latex/slb-obdh2-doc-*.pdf

  build_presentation:
    name: Build Presentation
    runs-on: ubuntu-latest

    steps:
      - name: Set up Git repository
        uses: actions/checkout@v6

      - name: Compile LaTeX document
        uses: xu-cheng/latex-action@v4
        with:
          working_directory: doc/presentation
          root_file: main.tex
